/*
NÃO REATIVE ESSA CLASSE!!!!!

NOT ENABLE THIS CLASS!!!!!
 */



/*package com.velp.bluetoothscanner;

import static androidx.core.app.ServiceCompat.startForeground;

import android.content.BroadcastReceiver;
import android.content.Context;
import android.content.Intent;
import android.content.pm.ServiceInfo;
import android.os.Build;

public class NotificationDismissedReceiver extends BroadcastReceiver {
    @Override
    public void onReceive(Context context, Intent intent) {
        if (Build.VERSION.SDK_INT >= Build.VERSION_CODES.UPSIDE_DOWN_CAKE) {
            startForeground(1, BluetoothScanService.getNotification(), ServiceInfo.FOREGROUND_SERVICE_TYPE_CONNECTED_DEVICE);
        } else {
            startForeground(1, BluetoothScanService.getNotification());
        }
    }
}*/
